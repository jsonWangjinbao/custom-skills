# API 规格 — {{需求名称}}

> 生成时间: {{当前日期}}
> 来源: {{源码逆向(重构) / 接口文档(增量)}}

## 1. API 总览

| 接口名 | URL | 用途 | 关联功能点 | 状态 |
|--------|-----|------|-----------|------|
| {{接口名}} | {{URL}} | {{用途描述}} | {{FP-xxx}} | {{available / spec-only / pending-backend}} |

## 2. 接口详表

### 2.1 {{接口名}}

**URL:** {{URL}}
**方法:** POST

#### 请求参数表

| 参数名 | 类型 | 必填 | 默认值 | 来源 | 说明 |
|--------|------|------|--------|------|------|
| {{参数名}} | {{类型}} | {{是/否}} | {{默认值}} | {{变量/常量}} | {{说明}} |

#### 响应字段表

> audit 阶段如发现本接口未覆盖的字段（apiSpecSupplements），回补到下表。

| 字段 | 类型 | 来源 API | 用途 | UI 元素 | 条件显隐 |
|------|------|----------|------|---------|---------|
| {{字段}} | {{类型}} | {{page/read/save}} | {{UI 用途描述}} | {{组件路径}} | {{条件}} |

#### 取值路径

| 平台 | 取值路径 |
|------|---------|
| RN | `res?.data?.content` |
| H5 | `res.content` |
| PC | `res?.data?.list` |

## 3. 字段综合映射表

| 业务字段 | page 接口 | read 接口 | save 接口 | 列表页 | 详情页 | 表单页 |
|---------|----------|----------|----------|--------|--------|--------|
| {{字段}} | {{✅/—}} | {{✅/—}} | {{✅/—}} | {{用途(FP-xx)}} | {{用途(FP-xx)}} | {{用途(FP-xx)}} |

## 4. Mock 数据

### {{接口名}} (status: spec-only / pending-backend)

```typescript
export const mock{{接口名PascalCase}} = {
code: 0,
content: [
{
// 字段必须与响应字段表一致，类型匹配，有业务语义的值
},
],
total_elements: 1,
};
```

## 5. API 状态清单

| 接口 | status | 备注 |
|------|--------|------|
| {{接口名}} | {{状态}} | {{备注}} |