# 执行文档 — {{需求名称}}

> 每完成一步，标记 ✅。
> 每完成一步，关联功能点同步勾掉。
> 当 `ui-audit.md` 状态为"完整"时，每个步骤必须同时完成功能代码和样式还原。
> 每步执行完成后在末尾追加 `实际: MM 分钟`，用于定位耗时瓶颈。
> **【强制】**当步骤涉及 UI 还原时，在 `实际` 行下方记录 `解析数据已读: parsed-styles/xxx.json`；回退读原始 HTML 时追加 `HTML 交叉验证: 文件路径`。
> 中断恢复时检查此日志，缺失则提示重新读取数据。

## 恢复入口

> 中断恢复时优先读取此区域定位续接位置，无需遍历全文。

- 当前进度：{{当前分组状态}}
- 下一步：{{下一步描述}}
- 最后更新：{{YYYY-MM-DD HH:mm}}

## 执行步骤

### 第一阶段: {{阶段名称}}

- [ ] Step 1: {{步骤描述}} → 关联: F-00X | UI参考: {{关联的 ui-audit 章节 / parsed-styles 文件 / 无(纯逻辑步骤)}} | 预计: {{时间}} |
  实际:
  解析数据已读: {{parsed-styles 文件路径 / 无(纯逻辑步骤)}}

- [ ] Step 2: {{步骤描述}} → 关联: F-00X | UI参考: {{关联的 ui-audit 章节 / parsed-styles 文件 / 无(纯逻辑步骤)}} | 预计: {{时间}} |
  实际:
  解析数据已读: {{parsed-styles 文件路径 / 无(纯逻辑步骤)}}

### 第二阶段: {{阶段名称}}

- [ ] Step N: {{步骤描述}} → 关联: F-00X | UI参考: {{关联的 ui-audit 章节 / parsed-styles 文件 / 无(纯逻辑步骤)}} | 预计: {{时间}} |
  实际:
  解析数据已读: {{parsed-styles 文件路径 / 无(纯逻辑步骤)}}

### 最终阶段: 自测

- [ ] Step N: 自测功能点 + 页面模式验证 + 组件映射验证 + 数据流验证 + 权限验证 + 路由验证 → 关联: 全部功能点

---

## 分组模式参照页

### 分组 1

| 维度 | 说明 | 参照页文件名 |
|------|------|-------------|
| 页面容器结构 | XlbPageContainer vs XlbProPageContainer | {{参照页路径}} |
| 搜索表单定义 | SearchFormType 配置模式 | {{参照页路径}} |
| API 响应取值 | res?.code === 0 后的数据路径 | {{参照页路径}} |
| Item props | data/onRefresh/onClose 命名 | {{参照页路径}} |
| 权限控制 | hasAuth 调用方式 | {{参照页路径}} |
| 列定义 | XlbTableColumnProps 使用 | {{参照页路径}} |
| 导航方式 | useIRouter / useNavigation | {{参照页路径}} |

### 分组 N

| 维度 | 说明 | 参照页文件名 |
|------|------|-------------|
| ... | ... | ... |

---

## 设计偏差记录

> 由 build 阶段 Step 5.5 自动生成。逐组记录设计偏差。

### 分组 1

| # | 元素 | 属性 | 预期 | 实际(代码) | 偏差类型 | 严重度 | 处理方案 |
|---|------|------|------|-----------|---------|--------|---------|
| {{1}} | {{元素}} | {{属性}} | {{预期值}} | {{实际值}} | {{类型}} | {{严重度}} | {{处理方案}} |

### 分组 N

| # | 元素 | 属性 | 预期 | 实际(代码) | 偏差类型 | 严重度 | 处理方案 |
|---|------|------|------|-----------|---------|--------|---------|
