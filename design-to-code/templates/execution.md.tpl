# 执行文档 — {{需求名称}}

> 每完成一步，标记 ✅。
> 每完成一步，关联功能点同步勾掉。
> 当 `ui-audit.md` 状态为"完整"时，每个步骤必须同时完成功能代码和样式还原。
> 每步执行完成后在末尾追加 `实际: MM 分钟`，用于定位耗时瓶颈。
> **【强制】**当步骤涉及 UI 还原时，必须在 `实际` 行下方记录 `HTML 已读: 文件路径1, 文件路径2`，作为执行日志。
> 中断恢复时检查此日志，缺失则提示重新读取 HTML。

## 执行步骤

### 第一阶段: {{阶段名称}}

- [ ] Step 1: {{步骤描述}} → 关联: F-00X | UI参考: {{关联的 ui-audit 章节 / HTML+截图文件名 / 无(纯逻辑步骤)}} | 样式合规: {{是/否}} | 预计: {{时间}} |
实际:
HTML 已读: {{HTML 文件路径 / 无(纯逻辑步骤)}}
- [ ] Step 2: {{步骤描述}} → 关联: F-00X | UI参考: {{关联的 ui-audit 章节 / HTML+截图文件名 / 无(纯逻辑步骤)}} | 样式合规: {{是/否}} | 预计: {{时间}} |
实际:
HTML 已读: {{HTML 文件路径 / 无(纯逻辑步骤)}}

### 第二阶段: {{阶段名称}}

- [ ] Step N: {{步骤描述}} → 关联: F-00X | UI参考: {{关联的 ui-audit 章节 / HTML+截图文件名 / 无(纯逻辑步骤)}} | 样式合规: {{是/否}} | 预计: {{时间}} |
实际:
HTML 已读: {{HTML 文件路径 / 无(纯逻辑步骤)}}

### 最终阶段: 自测

- [ ] Step N: 自测功能点 + UI还原验证 + 样式合规扫描 → 关联: 全部功能点